load("//rules/prettier/bzl:rules.bzl", "prettier", "prettier_binary")

package(default_visibility = ["//visibility:public"])

prettier_binary(
    name = "bin",
    dep = "@better_rules_javascript_npm//prettier:js",
)

prettier(
    name = "prettier",
    bin = ":bin",
    config = "//:prettierrc.yml"
)
